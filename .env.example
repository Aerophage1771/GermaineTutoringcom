# Database (use one; direct Postgres connection string)
# For Supabase use the Transaction pooler URI from Project Settings → Database
DATABASE_URL=
# SUPABASE_DB_URL=

# Session signing (use a long random string in production)
SESSION_SECRET=

# Optional: port when 5000 is in use
# PORT=5001

# Client-side Supabase (required for auth and messages from the browser)
# Get these from Supabase Dashboard → Project Settings → API
# Restart the dev server after changing these.
# Key: use either VITE_SUPABASE_ANON_KEY or VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY (Supabase may show "publishable" label)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# Optional: set to "dashboard" to show Login at /
# VITE_APP_MODE=
